package model;

public enum ShipType {
    AIRCRAFT_CARRIER,
    BATTLESHIP,
    SUBMARINE,
    CRUISER,
    DESTROYER
}

// enum to make 5 types of ships, useful since you only have to declare once